package Demo;

public class WebServiceTestingRestAPI {

	
	//public class WebServiceTest extends RestWSTestCase {
//		@QAFTestStep(description = "User do post new order for {0} with amount {1}")
//        public void addOrder(String clientName, String amount) throws Throwable {
//                  String data = String.format("{'clientName':'%s','amount':'%s'}", clientName, amount);
//                  getClient().resource(getBundle().getString("env.baseurl") + "/orders.json").post(data);
//      }
//     @QAFTestStep(description = "User deletes order with id {0}")
//     public void deleteOrder(String orderId) {
//               String resource = String.format("/orders/%s.json", orderId);
//               getClient().resource(getBundle().getString("env.baseurl") + resource).delete();
//    }
//   @QAFTestStep(description = "Order exist with id {0}")
//    public static void getOrder(String orderId) {
//              String resource = String.format("/orders/%s.json", orderId);
//              requestForResource(resource);
//              response_should_have_status("OK");
//   }
}
